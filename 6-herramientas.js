/**
 * Herramienta NodeMon > tools que observa cambios de los procesos o codigo que estamos ejecutando
 * en todos los archivos (compila, ejecutar)
 * npm install -g nodem -- ideal para desarrollo | nodemon app.js
 * npm install pm2 -g  -- ideal para produccion Monitoria toda la aplicacion | pm2 start app.js
 */

console.log('Nodemon!');